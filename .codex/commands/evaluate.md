---
description: Grade implementation against a plan's contract using an isolated adversarial evaluator
argument-hint: <path-to-plan.md>
---

Run the migrated `source-command-evaluate` skill with arguments: $ARGUMENTS

Treat this slash command as the Codex entrypoint for the former Claude project command `evaluate.md`.
