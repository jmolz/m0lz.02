---
description: Trigger a platform redeploy with an empty commit when webhooks or CI connections fail
---

Run the migrated `source-command-empty-redeploy` skill with arguments: $ARGUMENTS

Treat this slash command as the Codex entrypoint for the former Claude project command `empty-redeploy.md`.
