# Feature: {Feature Name}

> Generated by the migrated `plan-feature` command. Read `AGENTS.md` and referenced files before implementing.

## Overview

{What this implements and why.}

## User Story

As a {user type}, I want to {action}, so that {benefit}.

## Success Criteria

- [ ] {Verifiable criterion}
- [ ] {Verifiable criterion}
- [ ] All validation commands pass.
- [ ] No regressions in existing behavior.

## Spec Traceability

- **Original source**: {direct user request / PRD section / issue / email or customer signal / manually supplied reference}
- **Source excerpts or stable references**: {quote or link only material supplied during planning; use "none supplied" when absent}
- **Source-to-task mapping**:
  - `{source requirement}` -> Task {N}: {implementation task}
- **Source-to-contract mapping**:
  - `{source requirement}` -> Contract criterion: `{criterion name}`
- **Prime boundary**: `prime` orients on the repository and current state; this approved plan and its contract are the traceability mechanism.
- **External-source boundary**: PICE records references supplied during planning. This feature does not discover, fetch, or infer external PRDs, issues, emails, or source systems automatically.

## Feature Metadata

- Type: New Capability / Enhancement / Refactor / Bug Fix
- Complexity: Low / Medium / High
- Systems affected: {list}
- New dependencies: {list or none}

## Worktree

- **Branch**: `feature/{kebab-case-name}`
- **Path**: `.worktrees/{kebab-case-name}`

## Context References

### Must-Read Files

- `path/to/file` - Why: {pattern or interface to follow}

### New Files to Create

- `path/to/new_file` - {purpose}

### Documentation

- [Link](url) - Why: {what is needed}

### Patterns to Follow

Naming:

```text
{actual examples}
```

Error handling:

```text
{actual examples}
```

Imports:

```text
{actual examples}
```

## Implementation Tasks

Execute in order.

### Task 1: {Descriptive Name}

File: `path/to/file`
Action: CREATE / MODIFY
Description: {what to do and why}
Depends on: none
Validate: `{command}`

### Task 2: {Descriptive Name}

File: `path/to/file`
Action: CREATE / MODIFY
Description: {what to do and why}
Depends on: Task 1
Validate: `{command}`

## Testing Strategy

### Unit Tests

- Where: `{path}`
- Pattern: {reference test}
- Minimum: happy path, edge case, and error case where applicable.

### Integration Tests

{Cross-module or API-level tests.}

### E2E Validation

1. Start application: `{command}`
2. {Step with expected result}
3. Verify: {observable result}

## Validation Commands

### Level 1: Syntax and Types

```bash
{lint command}
{type-check command}
```

### Level 2: Unit Tests

```bash
{test command}
```

### Level 3: Build

```bash
{build command}
```

### Level 4: Manual Verification

{Exact steps, URLs, actions, and expected results.}

## Acceptance Criteria

- [ ] Tasks completed in dependency order.
- [ ] Validation commands pass.
- [ ] Manual verification confirms the feature.
- [ ] Code follows `AGENTS.md`.
- [ ] No regressions in existing behavior.

## Contract

```json
{
  "feature": "{Feature Name}",
  "tier": 1,
  "pass_threshold": 8,
  "criteria": [
    {
      "name": "{Specific, testable criterion}",
      "threshold": 8,
      "validation": "{Exact command, URL, or observable behavior}"
    },
    {
      "name": "{Negative criterion}",
      "threshold": 8,
      "validation": "{How to verify the bad thing does not happen}"
    }
  ]
}
```

## Adversarial Review

**Tier**: {1 | 2 | 3}
**Reviewers**: {fresh isolated reviewer(s)}
**Refinement cycles**: {N}

### Critical

1. {finding} - resolution: {revision or acknowledged reasoning}

### Consider

1. {finding}

### Reasoning Divergence

1. {alternative path} - why not: {reasoning}

## Notes

{Tradeoffs, follow-up work, known limitations.}
