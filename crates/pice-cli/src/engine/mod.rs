// v0.1 modules — output and status are dead code after T23 rewrites commands
// to go through the adapter. Retained for unit tests and as reference for the
// daemon handler port.
#[allow(dead_code)]
pub mod output;
#[allow(dead_code)]
pub mod status;
