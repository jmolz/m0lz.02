# PICE Project Learnings

PICE summary memory is disabled by default. If enabled with `store = "project_learnings"`,
redacted summary records may be appended below in machine-readable `pice-memory` blocks.
Keep this file free of secrets and raw tool output if your project commits `.pice/`.

